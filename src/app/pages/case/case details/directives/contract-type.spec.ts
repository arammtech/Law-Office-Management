import { ContractType } from './contract-type';

describe('ContractType', () => {
  it('should create an instance', () => {
    const directive = new ContractType();
    expect(directive).toBeTruthy();
  });
});
