import { CaseStatus } from './case-status';

describe('CaseStatus', () => {
  it('should create an instance', () => {
    const directive = new CaseStatus();
    expect(directive).toBeTruthy();
  });
});
