import { DialogParentClass } from './dialog-parent-class';

describe('DialogParentClass', () => {
  it('should create an instance', () => {
    expect(new DialogParentClass()).toBeTruthy();
  });
});
